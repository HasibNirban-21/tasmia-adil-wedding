# Wedding Invitation — Tasmia & Adil

A single-page, mobile-first wedding invitation website. Open `index.html` in any browser.

All wedding details (names, dates, venue, map link, programme, RSVP contact) live in one
`weddingConfig` object near the top of the `<script>` block in `index.html`.
